#include <stdio.h>

int main() {
    int num,bit;
    scanf("%d %d",&num,&bit);
    int result = (num>>bit) & 1;
    if (result==n){
        printf("%d",result);
    }
    else{
        printf("%d",result)
    }
    return 0;
}